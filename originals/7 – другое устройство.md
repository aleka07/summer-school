# 7 день — подключаем внешний источник данных

До этого мы сами генерировали данные для нашей лампочки. Это было удобно, потому что у нас нет реального IoT устройства на летней школе.

Теперь логика другая. Мы подключаем **внешний источник данных**. В нашем случае это публичный MQTT broker от TDengine, который постоянно публикует данные солнечных электростанций.

Ссылка:
https://tdengine.com/free-public-mqtt-data-feed-real-time-solar-fleet-simulation-now-available/

![[Pasted image 20260429164459.png]]

Идея занятия:

> У вас может быть любой внешний источник данных: MQTT broker, OPC UA, HTTP API, Modbus, Kafka, база данных, файл, что угодно.
>
> Главное — сделать адаптер, который переводит данные вашего источника в формат OpenEgiz/OpenTwins.
>
> Ваш источник → ваш адаптер → ваш цифровой двойник.

---

## Что у нас уже есть в OpenEgiz

OpenEgiz уже установил внутри себя Mosquitto MQTT broker.

Наши старые генераторы работали вот так:

```text
Python generator
  ↓
OpenEgiz Mosquitto localhost:30511, topic telemetry/...
  ↓
Eclipse Ditto / OpenTwins
  ↓
InfluxDB
  ↓
Grafana
```

Поэтому **новый MQTT broker поднимать не надо**.

У нас уже есть broker внутри OpenEgiz. Нам нужно поднять только один новый Python скрипт, который будет работать как bridge/adaptor.

Новая схема:

```text
TDengine public MQTT broker
mqtt.tdengine.com:1883
  ↓
наш Python bridge
  ↓
OpenEgiz Mosquitto localhost:30511
  ↓
OpenTwins digital twin
  ↓
InfluxDB + Grafana
```

То есть мы не заменяем OpenEgiz. Мы просто подключаем внешний поток данных к нашей платформе.

---

## Что дает TDengine

TDengine дает публичный MQTT broker:

```text
Broker: mqtt.tdengine.com
Port: 1883
Auth: не нужен
QoS: 0
```

Топики:

```text
sites      — данные уровня солнечной станции
inverters  — данные инверторов
strings    — данные солнечных string'ов
weather    — погодные станции
grid       — сетевые счетчики
```

Для занятия берем `sites`, потому что там проще всего объяснить:

- одна солнечная станция = один цифровой двойник
- мощность, генерация, эффективность, аварии = features двойника
- дальше это можно красиво показать в Grafana

Внутри `sites` приходит 5 станций:

```text
SITE_001
SITE_002
SITE_003
SITE_004
SITE_005
```

Для первого теста можно взять только `SITE_001`, чтобы проверить что все работает.

Для нормального занятия лучше включить `all`, и тогда будет 5 цифровых двойников:

```text
summerschool:solar-site-001
summerschool:solar-site-002
summerschool:solar-site-003
summerschool:solar-site-004
summerschool:solar-site-005
```

Почему я сначала говорил про `SITE_001`: это просто самый быстрый вариант для проверки. Но правильная логика занятия — показать, что один внешний источник может создавать поток данных сразу для нескольких двойников.

---

## 1. Проверяем внешний MQTT источник

Заходим на сервер, где работает OpenEgiz.

Если `mosquitto_sub` еще нет:

```bash
sudo apt update
sudo apt install mosquitto-clients
```

Проверяем, что TDengine реально отдает данные:

```bash
mosquitto_sub -h mqtt.tdengine.com -p 1883 -t 'sites' -v
```

Должно начать сыпаться что-то такое:

```json
sites {"ts":"2025-12-04T18:30:45.123456+00:00","Site_ID":"SITE_001","Fleet":"california","AC_Power_MW":16.84}
```

Чтобы остановить:

```bash
Ctrl+C
```

---

## 2. Создаем цифровые двойники в OpenTwins

Заходим в Grafana/OpenEgiz:

```text
http://SERVER_IP:30718
```

Дальше:

```text
OpenTwins → Twins → New Twin
```

Создаем первый twin:

```text
Namespace: summerschool
ID: solar-site-001
Policy ID: basic_policy
Name: Solar Site 001
Description: Внешний MQTT источник: TDengine solar fleet, SITE_001
Image URL: 
https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTReAo16vimjbNB30Y6XoHM96vtFZ2FaIaRYA&s
```

Attributes:

```text
asset_type: solar_site
source: TDengine public MQTT
external_id: SITE_001
fleet: california
```

Features:

```text
ac_power_mw
expected_power_mw
performance_ratio
availability_percent
curtailment_percent
energy_today_mwh
poa_irradiance_wm2
soiling_index
active_alarms
```

Create.

Если хотим сразу все 5 станций, повторяем для:

```text
summerschool:solar-site-002  external_id: SITE_002
summerschool:solar-site-003  external_id: SITE_003
summerschool:solar-site-004  external_id: SITE_004
summerschool:solar-site-005  external_id: SITE_005
```

Названия:

```text
Solar Site 002
Solar Site 003
Solar Site 004
Solar Site 005
```

Features везде одинаковые.

---

## 3. Создаем bridge скрипт

На сервере переходим в папку OpenEgiz:

```bash
cd ~/test/openegiz
```

Переходим в папку генераторов:

```bash
cd data-generator
```

Если venv уже есть:

```bash
source venv/bin/activate
```

Если venv нет:

```bash
python3 -m venv venv
source venv/bin/activate
pip install paho-mqtt
```

Создаем файл:

```bash
nano tdengine_solar_bridge.py
```

Вставляем код:

```python
#!/usr/bin/env python3
import argparse
import json
import random
import signal
import sys
import time
from datetime import datetime, timezone

import paho.mqtt.client as mqtt


TDENGINE_HOST = "mqtt.tdengine.com"
TDENGINE_PORT = 1883
TDENGINE_TOPIC = "sites"

OPENGIZ_MQTT_HOST = "localhost"
OPENGIZ_MQTT_PORT = 30511


SITE_TO_THING = {
    "SITE_001": "summerschool:solar-site-001",
    "SITE_002": "summerschool:solar-site-002",
    "SITE_003": "summerschool:solar-site-003",
    "SITE_004": "summerschool:solar-site-004",
    "SITE_005": "summerschool:solar-site-005",
}


def site_to_features(data):
    now = datetime.now(timezone.utc).isoformat()

    mapping = {
        "AC_Power_MW": "ac_power_mw",
        "Expected_Power_MW": "expected_power_mw",
        "Performance_Ratio": "performance_ratio",
        "Availability_%": "availability_percent",
        "Curtailment_%": "curtailment_percent",
        "Energy_Today_MWh": "energy_today_mwh",
        "POA_Irradiance_Wm2": "poa_irradiance_wm2",
        "Soiling_Index": "soiling_index",
        "Active_Alarms": "active_alarms",
    }

    features = {}

    for source_key, feature_name in mapping.items():
        if source_key in data:
            features[feature_name] = {
                "properties": {
                    "value": data[source_key],
                    "timestamp": data.get("ts", now),
                }
            }

    # Дополнительно сохраняем статус источника как feature.
    features["source_status"] = {
        "properties": {
            "value": "online",
            "source": "TDengine public MQTT",
            "topic": TDENGINE_TOPIC,
            "timestamp": now,
        }
    }

    return features


def build_ditto_message(thing_id, features):
    namespace, name = thing_id.split(":", 1)

    return {
        "topic": f"{namespace}/{name}/things/twin/commands/modify",
        "path": "/features",
        "value": features,
    }


class SolarBridge:
    def __init__(self, site_id, openegiz_host, openegiz_port):
        self.site_id = site_id
        self.openegiz_host = openegiz_host
        self.openegiz_port = openegiz_port
        self.running = True
        self.count_in = 0
        self.count_out = 0

        self.out_client = mqtt.Client(
            mqtt.CallbackAPIVersion.VERSION2,
            client_id=f"openegiz-solar-out-{random.randint(1000, 9999)}",
            protocol=mqtt.MQTTv5,
        )

        self.in_client = mqtt.Client(
            mqtt.CallbackAPIVersion.VERSION2,
            client_id=f"openegiz-solar-in-{random.randint(1000, 9999)}",
        )

        self.in_client.on_connect = self.on_connect
        self.in_client.on_message = self.on_message
        self.in_client.on_disconnect = self.on_disconnect

    def connect(self):
        print(f"Подключаемся к OpenEgiz MQTT: {self.openegiz_host}:{self.openegiz_port}")
        self.out_client.connect(self.openegiz_host, self.openegiz_port, keepalive=60)
        self.out_client.loop_start()

        print(f"Подключаемся к TDengine MQTT: {TDENGINE_HOST}:{TDENGINE_PORT}")
        self.in_client.connect(TDENGINE_HOST, TDENGINE_PORT, keepalive=60)

    def on_connect(self, client, userdata, flags, reason_code, properties):
        print(f"TDengine connected: {reason_code}")
        client.subscribe(TDENGINE_TOPIC, qos=0)
        print(f"Слушаем topic: {TDENGINE_TOPIC}")

    def on_disconnect(self, client, userdata, flags, reason_code, properties):
        print(f"TDengine disconnected: {reason_code}")

    def on_message(self, client, userdata, msg):
        try:
            self.count_in += 1
            payload = msg.payload.decode("utf-8")
            data = json.loads(payload)

            external_site_id = data.get("Site_ID")
            if not external_site_id:
                return

            if self.site_id != "all" and external_site_id != self.site_id:
                return

            thing_id = SITE_TO_THING.get(external_site_id)
            if not thing_id:
                return

            features = site_to_features(data)
            ditto_message = build_ditto_message(thing_id, features)

            openegiz_topic = f"telemetry/{thing_id}"
            result = self.out_client.publish(
                openegiz_topic,
                json.dumps(ditto_message),
                qos=0,
            )
            result.wait_for_publish(timeout=5)
            self.count_out += 1

            ac_power = data.get("AC_Power_MW")
            pr = data.get("Performance_Ratio")
            alarms = data.get("Active_Alarms")

            print(
                f"{datetime.now().strftime('%H:%M:%S')} "
                f"{external_site_id} -> {thing_id} | "
                f"AC={ac_power} MW | PR={pr} | alarms={alarms}"
            )

        except Exception as e:
            print(f"Ошибка обработки сообщения: {e}")

    def loop(self):
        self.in_client.loop_start()

        try:
            while self.running:
                time.sleep(1)
        finally:
            self.stop()

    def stop(self):
        print("Останавливаем bridge...")
        self.in_client.loop_stop()
        self.out_client.loop_stop()
        self.in_client.disconnect()
        self.out_client.disconnect()
        print(f"Получено сообщений: {self.count_in}")
        print(f"Отправлено в OpenEgiz: {self.count_out}")


def main():
    parser = argparse.ArgumentParser(
        description="Bridge: TDengine public MQTT -> OpenEgiz/OpenTwins"
    )
    parser.add_argument(
        "--site-id",
        default="SITE_001",
        help="SITE_001, SITE_002, SITE_003, SITE_004, SITE_005 или all",
    )
    parser.add_argument(
        "--openegiz-mqtt-host",
        default=OPENGIZ_MQTT_HOST,
        help="Адрес OpenEgiz Mosquitto",
    )
    parser.add_argument(
        "--openegiz-mqtt-port",
        type=int,
        default=OPENGIZ_MQTT_PORT,
        help="Порт OpenEgiz Mosquitto",
    )

    args = parser.parse_args()

    if args.site_id != "all" and args.site_id not in SITE_TO_THING:
        print("site-id должен быть SITE_001..SITE_005 или all")
        sys.exit(1)

    bridge = SolarBridge(
        site_id=args.site_id,
        openegiz_host=args.openegiz_mqtt_host,
        openegiz_port=args.openegiz_mqtt_port,
    )

    def handle_stop(sig, frame):
        bridge.running = False

    signal.signal(signal.SIGINT, handle_stop)
    signal.signal(signal.SIGTERM, handle_stop)

    bridge.connect()
    bridge.loop()


if __name__ == "__main__":
    main()
```

Сохраняем:

```text
Ctrl+O → Enter → Ctrl+X
```

Делаем файл исполняемым:

```bash
chmod +x tdengine_solar_bridge.py
```

---

## 4. Запускаем сначала один сайт

Для проверки запускаем только `SITE_001`:

```bash
python3 tdengine_solar_bridge.py --site-id SITE_001
```

Если все хорошо, в терминале будет что-то такое:

```text
12:40:05 SITE_001 -> summerschool:solar-site-001 | AC=16.84 MW | PR=0.977 | alarms=0
```

Теперь проверяем, что данные дошли до Ditto:

```bash
curl -s -u ditto:ditto \
  http://localhost:30525/api/2/things/summerschool:solar-site-001/features \
  | python3 -m json.tool
```

Если видим features и значения — все работает.

Останавливаем:

```bash
Ctrl+C
```

---

## 5. Запускаем все 5 солнечных станций

Если создали все 5 twins в OpenTwins, запускаем:

```bash
python3 tdengine_solar_bridge.py --site-id all
```

Теперь один внешний MQTT источник будет обновлять 5 цифровых двойников.

Проверка:

```bash
curl -s -u ditto:ditto \
  http://localhost:30525/api/2/things/summerschool:solar-site-003/features \
  | python3 -m json.tool
```

---

## 6. Запуск в фоне

Чтобы bridge не занимал терминал:

```bash
nohup python3 tdengine_solar_bridge.py --site-id all > tdengine_solar_bridge.log 2>&1 &
```

Смотрим логи:

```bash
tail -f tdengine_solar_bridge.log
```

Остановить:

```bash
ps aux | grep tdengine_solar_bridge.py
kill PID
```

где `PID` — номер процесса.

---

## 7. Дашборд в Grafana

Теперь нам нужно сделать простой дашборд для одной станции.

Работаем только с:

```text
summerschool:solar-site-001
```

Не делаем много графиков. Нам нужно показать главное:

```text
сколько станция генерирует сейчас
сколько она должна была генерировать
насколько хорошо она работает
есть ли аварии
```

Итого делаем 4 панели:

```text
1. Stat: AC Power
2. Stat: Performance Ratio
3. Stat: Active Alarms
4. Time series: Actual Power vs Expected Power
```

Заходим:

```text
Dashboards → New dashboard → Add visualization
```

Выбираем InfluxDB.

---

## 7.1 Stat — AC Power

Это текущая мощность станции.

Тип панели:

```text
Stat
```

Query:

```flux
from(bucket: "default")
  |> range(start: -60s)
  |> filter(fn: (r) => r["thingId"] == "summerschool:solar-site-001")
  |> filter(fn: (r) => r["_field"] == "value_ac_power_mw_properties_value")
  |> last()
  |> map(fn: (r) => ({r with _field: "AC Power (MW)"}))
```

Unit:

```text
MW
```

---

## 7.2 Stat — Performance Ratio

Это показатель эффективности станции.

Тип панели:

```text
Stat
```

```flux
from(bucket: "default")
  |> range(start: -60s)
  |> filter(fn: (r) => r["thingId"] == "summerschool:solar-site-001")
  |> filter(fn: (r) => r["_field"] == "value_performance_ratio_properties_value")
  |> last()
  |> map(fn: (r) => ({r with _field: "Performance Ratio"}))
```

Thresholds:

```text
0.00 - 0.95  red
0.95 - 0.98  yellow
0.98 - 1.00  green
```

---

## 7.3 Stat — Active Alarms

Это количество активных аварий.

Тип панели:

```text
Stat
```

```flux
from(bucket: "default")
  |> range(start: -60s)
  |> filter(fn: (r) => r["thingId"] == "summerschool:solar-site-001")
  |> filter(fn: (r) => r["_field"] == "value_active_alarms_properties_value")
  |> last()
  |> map(fn: (r) => ({r with _field: "Active Alarms"}))
```

Thresholds:

```text
0 green
1 yellow
2 red
```

---

## 7.4 Time series — Actual Power vs Expected Power

Это самый важный график.

Он показывает: станция реально выдает столько, сколько должна, или нет.

Тип панели:

```text
Time series
```

Query A — фактическая мощность:

```flux
from(bucket: "default")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["thingId"] == "summerschool:solar-site-001")
  |> filter(fn: (r) => r["_field"] == "value_ac_power_mw_properties_value")
  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)
  |> map(fn: (r) => ({r with _field: "Actual AC Power"}))
```

Query B — ожидаемая мощность:

```flux
from(bucket: "default")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["thingId"] == "summerschool:solar-site-001")
  |> filter(fn: (r) => r["_field"] == "value_expected_power_mw_properties_value")
  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)
  |> map(fn: (r) => ({r with _field: "Expected Power"}))
```

Unit:

```text
MW
```

В идеале две линии должны идти рядом. Если actual сильно ниже expected — значит станция недодает мощность.

---

## 7.5 Как должен выглядеть итоговый dashboard

Я бы сделал так:

```text
Row 1:
[AC Power] [Performance Ratio] [Active Alarms]

Row 2:
[Actual AC Power vs Expected Power]
```

Этого достаточно для 7-го дня.

Главное показать:

```text
внешний MQTT источник → bridge → OpenEgiz → Grafana
```

А уже в ML дне мы добавим прогнозы и anomaly score.

---

## Главная мысль дня

Сегодня мы впервые подключили не наш генератор, а внешний источник данных.

Важно понять не TDengine, а сам принцип:

```text
любой внешний источник данных
  ↓
адаптер
  ↓
формат OpenEgiz/OpenTwins
  ↓
цифровой двойник
```

Сегодня внешний источник — публичный MQTT от TDengine.

Завтра это может быть:

- реальный MQTT broker предприятия
- датчик ESP32
- API погоды
- OPC UA сервер
- Modbus устройство
- Kafka topic
- файл с историческими данными

Логика всегда одна:

```text
ваш источник → ваш двойник
```
